import React, { Component } from 'react';
import {
	AppRegistry
} from 'react-native';
import AppContainer from './app/AppContainer'

AppRegistry.registerComponent('WeeklyGroceriesList', () => AppContainer);